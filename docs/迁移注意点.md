
### 1. 去掉了 base-datarule 依赖

        <dependency>
            <groupId>com.xiaoniu</groupId>
            <artifactId>base-datarule</artifactId>
            <version>${base-datarule.version}</version>
            <exclusions>
                <exclusion>
                    <groupId>com.xiaoniu</groupId>
                    <artifactId>xiaoniu-idempotent</artifactId>
                </exclusion>
                <exclusion>
                    <artifactId>xiaoniu-common</artifactId>
                    <groupId>com.xiaoniu</groupId>
                </exclusion>
            </exclusions>
        </dependency>


### 2. report依赖的问题
合同旧服务 report依赖的是716,目前迁移的也是716,为了兼容不报错 创建了CustomThreadPoolTaskExecutor 同名类

### 3.修改mybatis配置
删除了 MyBatisPageInterceptor、MybatisCommonMysqlConfig、MybatisMysqlConfig
为了兼容不报错 创建了MybatisPlusPageConfig，后续去掉xiaoniu-common依赖后再做修改



